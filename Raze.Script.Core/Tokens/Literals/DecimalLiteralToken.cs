namespace Raze.Script.Core.Tokens.Literals;

internal class DecimalLiteralToken : LiteralToken
{
    public DecimalLiteralToken(string lexeme, int line, int column)
        : base(lexeme, line, column)
    {
    }
}
