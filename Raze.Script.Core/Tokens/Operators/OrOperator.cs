namespace Raze.Script.Core.Tokens.Operators;

internal class OrOperator : OperatorToken
{
    public OrOperator(string lexeme, int line, int column)
        : base(lexeme, line, column)
    {
    }
}
