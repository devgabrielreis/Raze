namespace Raze.Script.Core.Tokens.ControlStructures;

internal class Continue : ControlStructureToken
{
    public Continue(string lexeme, int line, int column)
        : base(lexeme, line, column)
    {
    }
}
