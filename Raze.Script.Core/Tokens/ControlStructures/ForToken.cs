namespace Raze.Script.Core.Tokens.ControlStructures;

internal class ForToken : ControlStructureToken
{
    public ForToken(string lexeme, int line, int column)
    : base(lexeme, line, column)
    {
    }
}
