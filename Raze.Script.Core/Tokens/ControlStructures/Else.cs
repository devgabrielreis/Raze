namespace Raze.Script.Core.Tokens.ControlStructures;

internal class Else : ControlStructureToken
{
    public Else(string lexeme, int line, int column)
    : base(lexeme, line, column)
    {
    }
}
