namespace Raze.Script.Core.Tokens.Delimiters;

internal class SemiColon : DelimiterToken
{
    public SemiColon(string lexeme, int line, int column)
        : base(lexeme, line, column)
    {
    }
}
